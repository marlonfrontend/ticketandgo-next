import { Container, Brand } from '@/components'

export const Header = () => {
  return (
    <header className="bg-primary py-4">
      <Container>
        <Brand />
      </Container>
    </header>
  )
}
